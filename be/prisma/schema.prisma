generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL")
}

model User {
  Id                                     String                  @id @default(uuid()) @db.UniqueIdentifier
  Email                                  String?                 @unique
  PasswordHash                           String?
  FullName                               String?
  StudentCode                            String?
  LecturerCode                           String?
  Phone                                  String?
  Avatar                                 String?
  Status                                 String?
  LastLoginAt                            DateTime?               @db.DateTime2
  RequirePasswordChange                  Boolean?                @default(false)
  ResetPasswordOtp                       String?
  ResetPasswordOtpExpiry                 DateTime?               @db.DateTime2
  AiUsageLog                             AiUsageLog[]
  AuditLog                               AuditLog[]
  Exam                                   Exam[]
  ExamGenerationHistory                  ExamGenerationHistory[]
  GradingSession                         GradingSession[]
  ImportBatch                            ImportBatch[]
  InstructorClass                        InstructorClass[]
  Notification                           Notification[]
  NotificationRecipient                  NotificationRecipient[]
  PromptTemplate                         PromptTemplate[]
  OAuthIdentity                          OAuthIdentity[]
  RefreshToken                           RefreshToken[]
  StudentClass                           StudentClass[]
  Submission_Submission_ReviewedByToUser Submission[]            @relation("Submission_ReviewedByToUser")
  Submission_Submission_StudentIdToUser  Submission[]            @relation("Submission_StudentIdToUser")
  UserRole                               UserRole[]
  Appeal                                 Appeal[]
  PendingEnrollment                      PendingEnrollment[]
  SubmissionOverride_Student             SubmissionOverride[]    @relation("SubmissionOverride_StudentIdToUser")
  SubmissionOverride_Creator             SubmissionOverride[]    @relation("SubmissionOverride_CreatedByToUser")
}

model Class {
  Id              String            @id @default(uuid()) @db.UniqueIdentifier
  ClassCode       String?
  SubjectId       String?           @db.UniqueIdentifier
  SemesterId      String?           @db.UniqueIdentifier
  Status          String?
  Note            String?
  Semester        Semester?         @relation(fields: [SemesterId], references: [Id], onDelete: Cascade, onUpdate: NoAction)
  Subject         Subject?          @relation(fields: [SubjectId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  InstructorClass InstructorClass[]
  StudentClass    StudentClass[]
  Submission      Submission[]
  ExamClass       ExamClass[]

  @@unique([ClassCode, SubjectId, SemesterId])
}

model Submission {
  Id                               String               @id @default(uuid()) @db.UniqueIdentifier
  ExamId                           String?              @db.UniqueIdentifier
  StudentId                        String?              @db.UniqueIdentifier
  ClassId                          String?              @db.UniqueIdentifier
  AttemptNumber                    Int?
  IsLatest                         Boolean?
  SubmittedAt                      DateTime?            @db.DateTime2
  ZipFileUrl                       String?
  GradingStatus                    String?
  ReviewStatus                     String?
  TotalScore                       Decimal?             @db.Decimal(18, 2)
  RawScore                         Decimal?             @db.Decimal(18, 2)
  LatePenaltyAmount                Decimal?             @db.Decimal(18, 2)
  FinalScore                       Decimal?             @db.Decimal(18, 2)
  IsReopened                       Boolean?             @default(false)
  ReopenReason                     String?
  InstructorFeedback               String?
  StudentFeedback                  String?
  ReviewedBy                       String?              @db.UniqueIdentifier
  ReviewedAt                       DateTime?            @db.DateTime2
  GradedAt                         DateTime?            @db.DateTime2
  ReportData                       String?              @db.NVarChar(Max)
  AiUsageLog                       AiUsageLog[]
  ExecutionResult                  ExecutionResult[]
  GradingSession                   GradingSession[]
  Appeal                           Appeal[]
  Class                            Class?               @relation(fields: [ClassId], references: [Id], onDelete: Cascade, onUpdate: NoAction)
  Exam                             Exam?                @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User_Submission_ReviewedByToUser User?                @relation("Submission_ReviewedByToUser", fields: [ReviewedBy], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User_Submission_StudentIdToUser  User?                @relation("Submission_StudentIdToUser", fields: [StudentId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  SubmissionArtifact               SubmissionArtifact[]

  @@unique([StudentId, ExamId, ClassId, AttemptNumber], map: "Submission_StudentId_ExamId_ClassId_AttemptNumber_idx")
}

model Subject {
  Id                 String               @id @default(uuid()) @db.UniqueIdentifier
  SubjectCode        String?              @unique
  SubjectName        String?
  Description        String?
  IsActive           Boolean?
  Semester           Int?
  Credit             Int?
  SyllabusData       String?              @db.NVarChar(Max)
  AssignmentTemplate AssignmentTemplate[]
  Class              Class[]
  Exam               Exam[]
  PromptTemplate     PromptTemplate[]
  SubjectProjectType SubjectProjectType[]
  SemesterSubject    SemesterSubject[]
}

model Notification {
  Id                    String                  @id @default(uuid()) @db.UniqueIdentifier
  Title                 String?
  Message               String?
  Type                  String?
  ReferenceId           String?                 @db.UniqueIdentifier
  ReferenceType         String?
  CreatedBy             String?                 @db.UniqueIdentifier
  CreatedAt             DateTime?               @db.DateTime2
  User                  User?                   @relation(fields: [CreatedBy], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  NotificationRecipient NotificationRecipient[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AiApiKey {
  Id              String    @id @default(uuid()) @db.UniqueIdentifier
  Provider        String?
  KeyValue        String?
  Label           String?
  IsActive        Boolean?
  IsExhausted     Boolean?
  LastUsedAt      DateTime? @db.DateTime2
  TotalUsageCount Int?
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AiUsageLog {
  Id               String      @id @default(uuid()) @db.UniqueIdentifier
  UserId           String?     @db.UniqueIdentifier
  ExamId           String?     @db.UniqueIdentifier
  SubmissionId     String?     @db.UniqueIdentifier
  RequestId        String?
  Provider         String?
  ActionType       String?
  ModelUsed        String?
  PromptTokens     Int?
  CompletionTokens Int?
  CostEstimate     Decimal?    @db.Decimal(18, 2)
  DurationMs       Int?
  IsSuccess        Boolean?
  ErrorMessage     String?
  CreatedAt        DateTime?   @db.DateTime2
  Exam             Exam?       @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  Submission       Submission? @relation(fields: [SubmissionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User             User?       @relation(fields: [UserId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AssignmentTemplate {
  Id                      String          @id @default(uuid()) @db.UniqueIdentifier
  SubjectId               String?         @db.UniqueIdentifier
  Name                    String?
  Description             String?
  AssignmentType          String?
  DefaultGradingProfileId String?         @db.UniqueIdentifier
  DefaultProjectTypeId    String?         @db.UniqueIdentifier
  SortOrder               Int?
  IsActive                Boolean?
  GradingProfile          GradingProfile? @relation(fields: [DefaultGradingProfileId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  ProjectType             ProjectType?    @relation(fields: [DefaultProjectTypeId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  Subject                 Subject?        @relation(fields: [SubjectId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  Exam                    Exam[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model AuditLog {
  Id         String    @id @default(uuid()) @db.UniqueIdentifier
  EntityName String?
  EntityId   String?   @db.UniqueIdentifier
  Action     String?
  OldValue   String?
  NewValue   String?
  UserId     String?   @db.UniqueIdentifier
  IpAddress  String?
  CreatedAt  DateTime? @db.DateTime2
  User       User?     @relation(fields: [UserId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model BuildArtifact {
  Id               String          @id @default(uuid()) @db.UniqueIdentifier
  GradingSessionId String?         @db.UniqueIdentifier
  BuildStatus      String?
  BuildLog         String?
  DockerImageId    String?
  ExecutablePath   String?
  BuildHash        String?
  DurationMs       Int?
  GradingSession   GradingSession? @relation(fields: [GradingSessionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model CriterionScore {
  Id                String           @id @default(uuid()) @db.UniqueIdentifier
  RuleScoreId       String?          @db.UniqueIdentifier
  RubricCriterionId String?          @db.UniqueIdentifier
  AiScore           Decimal?         @db.Decimal(18, 2)
  MaxScore          Decimal?         @db.Decimal(18, 2)
  AiReasoning       String?
  InstructorScore   Decimal?         @db.Decimal(18, 2)
  IsOverridden      Boolean?
  EffectiveScore    Decimal?         @db.Decimal(18, 2)
  RubricCriterion   RubricCriterion? @relation(fields: [RubricCriterionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  RuleScore         RuleScore?       @relation(fields: [RuleScoreId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Evidence {
  Id           String     @id @default(uuid()) @db.UniqueIdentifier
  RuleScoreId  String?    @db.UniqueIdentifier
  EvidenceType String?
  Content      String?
  ImageUrl     String?
  Caption      String?
  RuleScore    RuleScore? @relation(fields: [RuleScoreId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Exam {
  Id                    String                  @id @default(uuid()) @db.UniqueIdentifier
  Title                 String?
  Description           String?
  AssignmentTemplateId  String?                 @db.UniqueIdentifier
  ProjectTypeId         String?                 @db.UniqueIdentifier
  GradingProfileId      String?                 @db.UniqueIdentifier
  SubjectId             String?                 @db.UniqueIdentifier
  ExamType              String?
  Duration              Int?
  StartDate             DateTime?               @db.DateTime2
  DueDate               DateTime?               @db.DateTime2
  TotalPoints           Decimal?                @db.Decimal(18, 2)
  WeightPercentage      Decimal?                @db.Decimal(5, 2)
  Status                String?
  GradingStrategy       String?                 @default("CONTINUOUS_QUEUE")
  SubmissionFormat      String?
  AiGeneratedContent    String?                 @db.NVarChar(Max)
  OriginalPrompt        String?                 @db.NVarChar(Max)
  PromptTemplateId      String?                 @db.UniqueIdentifier
  CreatedBy             String?                 @db.UniqueIdentifier
  LatePenaltyType       String?                 @default("NONE")
  LatePenaltyValue      Decimal?                @db.Decimal(18, 2)
  MaxLatePenalty        Decimal?                @db.Decimal(18, 2)
  AllowLateSubmission   Boolean?                @default(true)
  IsDeleted             Boolean?                @default(false)
  DeletedAt             DateTime?               @db.DateTime2
  AiUsageLog            AiUsageLog[]
  AssignmentTemplate    AssignmentTemplate?     @relation(fields: [AssignmentTemplateId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User                  User?                   @relation(fields: [CreatedBy], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  GradingProfile        GradingProfile?         @relation(fields: [GradingProfileId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  ProjectType           ProjectType?            @relation(fields: [ProjectTypeId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  PromptTemplate        PromptTemplate?         @relation(fields: [PromptTemplateId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  Subject               Subject?                @relation(fields: [SubjectId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  ExamAttachment        ExamAttachment[]
  ExamGenerationHistory ExamGenerationHistory[]
  ExamSection           ExamSection[]
  ReferenceArtifact     ReferenceArtifact[]
  SampleCode            SampleCode[]
  Submission            Submission[]
  TestCase              TestCase[]
  ExamClass             ExamClass[]
  SubmissionOverride    SubmissionOverride[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ExamAttachment {
  Id       String  @id @default(uuid()) @db.UniqueIdentifier
  ExamId   String? @db.UniqueIdentifier
  FileName String?
  FileUrl  String?
  FileType String?
  Exam     Exam?   @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ExamGenerationHistory {
  Id               String  @id @default(uuid()) @db.UniqueIdentifier
  ExamId           String? @db.UniqueIdentifier
  Version          Int?
  Prompt           String? @db.NVarChar(Max)
  GeneratedContent String? @db.NVarChar(Max)
  GeneratedBy      String? @db.UniqueIdentifier
  Exam             Exam?   @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User             User?   @relation(fields: [GeneratedBy], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ExamSection {
  Id          String       @id @default(uuid()) @db.UniqueIdentifier
  ExamId      String?      @db.UniqueIdentifier
  SectionCode String?
  Title       String?
  Description String?
  MaxPoints   Decimal?     @db.Decimal(18, 2)
  SortOrder   Int?
  Exam        Exam?        @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  RubricRule  RubricRule[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ExecutionResult {
  Id             String      @id @default(uuid()) @db.UniqueIdentifier
  GradingJobId   String?     @db.UniqueIdentifier
  SubmissionId   String?     @db.UniqueIdentifier
  EngineType     String?
  ExecutionLabel String?
  RawOutput      String?
  Score          Decimal?    @db.Decimal(18, 2)
  Status         String?
  StartedAt      DateTime?   @db.DateTime2
  CompletedAt    DateTime?   @db.DateTime2
  DurationMs     Int?
  GradingJob     GradingJob? @relation(fields: [GradingJobId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  Submission     Submission? @relation(fields: [SubmissionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  RuleScore      RuleScore[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model GradingJob {
  Id                                                     String              @id @default(uuid()) @db.UniqueIdentifier
  GradingSessionId                                       String?             @db.UniqueIdentifier
  GradingProfileStepId                                   String?             @db.UniqueIdentifier
  Engine                                                 String?
  Priority                                               Int?
  Status                                                 String?
  TriggeredAt                                            DateTime?           @db.DateTime2
  CompletedAt                                            DateTime?           @db.DateTime2
  WorkerNodeId                                           String?
  ExecutionResult                                        ExecutionResult[]
  GradingProfileStep                                     GradingProfileStep? @relation(fields: [GradingProfileStepId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  GradingSession                                         GradingSession?     @relation(fields: [GradingSessionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  JobDependency_JobDependency_DependsOnJobIdToGradingJob JobDependency[]     @relation("JobDependency_DependsOnJobIdToGradingJob")
  JobDependency_JobDependency_JobIdToGradingJob          JobDependency[]     @relation("JobDependency_JobIdToGradingJob")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model GradingProfile {
  Id                 String               @id @default(uuid()) @db.UniqueIdentifier
  Name               String?
  ProjectTypeId      String?              @db.UniqueIdentifier
  Description        String?
  IsActive           Boolean?
  AssignmentTemplate AssignmentTemplate[]
  Exam               Exam[]
  ProjectType        ProjectType?         @relation(fields: [ProjectTypeId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  GradingProfileStep GradingProfileStep[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model GradingProfileStep {
  Id               String          @id @default(uuid()) @db.UniqueIdentifier
  GradingProfileId String?         @db.UniqueIdentifier
  EngineType       String?
  StepOrder        Int?
  ConfigJson       String?
  IsRequired       Boolean?
  TimeoutSeconds   Int?
  GradingJob       GradingJob[]
  GradingProfile   GradingProfile? @relation(fields: [GradingProfileId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model GradingSession {
  Id               String             @id @default(uuid()) @db.UniqueIdentifier
  SubmissionId     String?            @db.UniqueIdentifier
  Version          Int?
  TriggeredBy      String?            @db.UniqueIdentifier
  TriggerReason    String?
  Status           String?
  TotalScore       Decimal?           @db.Decimal(18, 2)
  StartedAt        DateTime?          @db.DateTime2
  CompletedAt      DateTime?          @db.DateTime2
  BuildArtifact    BuildArtifact[]
  GradingJob       GradingJob[]
  Submission       Submission?        @relation(fields: [SubmissionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User             User?              @relation(fields: [TriggeredBy], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  SandboxExecution SandboxExecution[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ImportBatch {
  Id           String  @id @default(uuid()) @db.UniqueIdentifier
  FileName     String?
  FileUrl      String?
  Status       String?
  TotalRows    Int?
  SuccessCount Int?
  ErrorCount   Int?
  ErrorDetails String? @db.NVarChar(Max)
  ImportedBy   String? @db.UniqueIdentifier
  User         User?   @relation(fields: [ImportedBy], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model InstructorClass {
  UserId     String    @db.UniqueIdentifier
  ClassId    String    @db.UniqueIdentifier
  EnrolledAt DateTime? @db.DateTime2
  Class      Class     @relation(fields: [ClassId], references: [Id], onDelete: Cascade, onUpdate: NoAction)
  User       User      @relation(fields: [UserId], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@id([UserId, ClassId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model JobDependency {
  JobId                                               String     @db.UniqueIdentifier
  DependsOnJobId                                      String     @db.UniqueIdentifier
  DependencyType                                      String?
  GradingJob_JobDependency_DependsOnJobIdToGradingJob GradingJob @relation("JobDependency_DependsOnJobIdToGradingJob", fields: [DependsOnJobId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  GradingJob_JobDependency_JobIdToGradingJob          GradingJob @relation("JobDependency_JobIdToGradingJob", fields: [JobId], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@id([JobId, DependsOnJobId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model NotificationRecipient {
  Id             String        @id @default(uuid()) @db.UniqueIdentifier
  NotificationId String?       @db.UniqueIdentifier
  UserId         String?       @db.UniqueIdentifier
  IsRead         Boolean?
  ReadAt         DateTime?     @db.DateTime2
  Notification   Notification? @relation(fields: [NotificationId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User           User?         @relation(fields: [UserId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ProjectType {
  Id                                                                             String                   @id @default(uuid()) @db.UniqueIdentifier
  Code                                                                           String?                  @unique
  DisplayName                                                                    String?
  RequiresSandbox                                                                Boolean?
  SandboxImage                                                                   String?
  BuildCommand                                                                   String?
  RunCommand                                                                     String?
  AssignmentTemplate                                                             AssignmentTemplate[]
  Exam                                                                           Exam[]
  GradingProfile                                                                 GradingProfile[]
  ProjectTypeComposition_ProjectTypeComposition_ChildProjectTypeIdToProjectType  ProjectTypeComposition[] @relation("ProjectTypeComposition_ChildProjectTypeIdToProjectType")
  ProjectTypeComposition_ProjectTypeComposition_ParentProjectTypeIdToProjectType ProjectTypeComposition[] @relation("ProjectTypeComposition_ParentProjectTypeIdToProjectType")
  PromptTemplate                                                                 PromptTemplate[]
  SubjectProjectType                                                             SubjectProjectType[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ProjectTypeComposition {
  ParentProjectTypeId                                                 String      @db.UniqueIdentifier
  ChildProjectTypeId                                                  String      @db.UniqueIdentifier
  Role                                                                String?
  IsRequired                                                          Boolean?
  SortOrder                                                           Int?
  ProjectType_ProjectTypeComposition_ChildProjectTypeIdToProjectType  ProjectType @relation("ProjectTypeComposition_ChildProjectTypeIdToProjectType", fields: [ChildProjectTypeId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  ProjectType_ProjectTypeComposition_ParentProjectTypeIdToProjectType ProjectType @relation("ProjectTypeComposition_ParentProjectTypeIdToProjectType", fields: [ParentProjectTypeId], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@id([ParentProjectTypeId, ChildProjectTypeId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model PromptTemplate {
  Id                String       @id @default(uuid()) @db.UniqueIdentifier
  Name              String?
  SubjectId         String?      @db.UniqueIdentifier
  ProjectTypeId     String?      @db.UniqueIdentifier
  Category          String?
  TemplateContent   String?      @db.NVarChar(Max)
  PlaceholderSchema String?      @db.NVarChar(Max)
  IsDefault         Boolean?
  IsActive          Boolean?
  UsageCount        Int?
  CreatedBy         String?      @db.UniqueIdentifier
  Exam              Exam[]
  User              User?        @relation(fields: [CreatedBy], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  ProjectType       ProjectType? @relation(fields: [ProjectTypeId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  Subject           Subject?     @relation(fields: [SubjectId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ReferenceArtifact {
  Id           String  @id @default(uuid()) @db.UniqueIdentifier
  ExamId       String? @db.UniqueIdentifier
  ArtifactType String?
  ExpectedFor  String?
  FileName     String?
  FileUrl      String?
  Description  String?
  SortOrder    Int?
  Exam         Exam?   @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

model RefreshToken {
  Id        String    @id @default(uuid()) @db.UniqueIdentifier
  UserId    String?   @db.UniqueIdentifier
  Token     String?
  ExpiresAt DateTime? @db.DateTime2
  IsRevoked Boolean?
  User      User?     @relation(fields: [UserId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

model OAuthIdentity {
  Id           String  @id @default(uuid()) @db.UniqueIdentifier
  UserId       String? @db.UniqueIdentifier
  Provider     String?
  ProviderId   String?
  AccessToken  String?
  RefreshToken String?
  User         User?   @relation(fields: [UserId], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([Provider, ProviderId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Role {
  Id       String     @id @default(uuid()) @db.UniqueIdentifier
  RoleName String?    @unique
  UserRole UserRole[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model RubricCriterion {
  Id               String           @id @default(uuid()) @db.UniqueIdentifier
  RubricRuleId     String?          @db.UniqueIdentifier
  Description      String?
  MaxPoints        Decimal?         @db.Decimal(18, 2)
  Weight           Decimal?         @db.Decimal(18, 2)
  ValidationType   String?
  ValidationConfig String?
  IsCritical       Boolean?
  SortOrder        Int?
  CriterionScore   CriterionScore[]
  RubricRule       RubricRule?      @relation(fields: [RubricRuleId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model RubricRule {
  Id                   String            @id @default(uuid()) @db.UniqueIdentifier
  SectionId            String?           @db.UniqueIdentifier
  RuleCode             String?
  Description          String?
  MaxPoints            Decimal?          @db.Decimal(18, 2)
  EvaluationPrompt     String?
  ReferenceAnswer      String?
  RequireImageEvidence Boolean?
  RequireCodeEvidence  Boolean?
  IsManualOnly         Boolean?
  SortOrder            Int?
  RubricCriterion      RubricCriterion[]
  ExamSection          ExamSection?      @relation(fields: [SectionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  RuleScore            RuleScore[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model RuleScore {
  Id                String           @id @default(uuid()) @db.UniqueIdentifier
  ExecutionResultId String?          @db.UniqueIdentifier
  RubricRuleId      String?          @db.UniqueIdentifier
  AiScore           Decimal?         @db.Decimal(18, 2)
  MaxScore          Decimal?         @db.Decimal(18, 2)
  AiReasoning       String?
  InstructorScore   Decimal?         @db.Decimal(18, 2)
  InstructorComment String?
  IsOverridden      Boolean?
  EffectiveScore    Decimal?         @db.Decimal(18, 2)
  CriterionScore    CriterionScore[]
  Evidence          Evidence[]
  ExecutionResult   ExecutionResult? @relation(fields: [ExecutionResultId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  RubricRule        RubricRule?      @relation(fields: [RubricRuleId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SampleCode {
  Id          String  @id @default(uuid()) @db.UniqueIdentifier
  ExamId      String? @db.UniqueIdentifier
  Language    String?
  Code        String?
  Explanation String?
  Exam        Exam?   @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SandboxExecution {
  Id               String          @id @default(uuid()) @db.UniqueIdentifier
  GradingSessionId String?         @db.UniqueIdentifier
  ContainerId      String?
  SandboxImage     String?
  Status           String?
  ExitCode         Int?
  CpuTimeMs        BigInt?
  PeakMemoryKb     BigInt?
  NetworkDisabled  Boolean?
  StartedAt        DateTime?       @db.DateTime2
  FinishedAt       DateTime?       @db.DateTime2
  RunLog           String?
  GradingSession   GradingSession? @relation(fields: [GradingSessionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Semester {
  Id              String            @id @default(uuid()) @db.UniqueIdentifier
  Season          String?
  Code            String?
  StartDate       DateTime?         @db.DateTime2
  EndDate         DateTime?         @db.DateTime2
  IsActive        Boolean?
  Class           Class[]
  SemesterSubject SemesterSubject[]

  @@unique([Season, Code])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SemesterSubject {
  SemesterId String   @db.UniqueIdentifier
  SubjectId  String   @db.UniqueIdentifier
  AssignedAt DateTime @default(now()) @db.DateTime2

  Semester Semester @relation(fields: [SemesterId], references: [Id], onDelete: Cascade, onUpdate: NoAction)
  Subject  Subject  @relation(fields: [SubjectId], references: [Id], onDelete: Cascade, onUpdate: NoAction)

  @@id([SemesterId, SubjectId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model StudentClass {
  UserId     String    @db.UniqueIdentifier
  ClassId    String    @db.UniqueIdentifier
  EnrolledAt DateTime? @db.DateTime2
  Class      Class     @relation(fields: [ClassId], references: [Id], onDelete: Cascade, onUpdate: NoAction)
  User       User      @relation(fields: [UserId], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@id([UserId, ClassId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SubjectProjectType {
  SubjectId     String      @db.UniqueIdentifier
  ProjectTypeId String      @db.UniqueIdentifier
  ProjectType   ProjectType @relation(fields: [ProjectTypeId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  Subject       Subject     @relation(fields: [SubjectId], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@id([SubjectId, ProjectTypeId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SubmissionArtifact {
  Id            String      @id @default(uuid()) @db.UniqueIdentifier
  SubmissionId  String?     @db.UniqueIdentifier
  ArtifactType  String?
  ArtifactLabel String?
  FileName      String?
  FileUrl       String?
  FileSizeBytes BigInt?
  HashSha256    String?
  ExtractedPath String?
  MimeType      String?
  Submission    Submission? @relation(fields: [SubmissionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model SystemConfig {
  Key         String  @id
  Value       String?
  Description String?
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model TestCase {
  Id             String   @id @default(uuid()) @db.UniqueIdentifier
  ExamId         String?  @db.UniqueIdentifier
  Input          String?
  ExpectedOutput String?
  IsHidden       Boolean?
  TimeoutMs      Int?
  MemoryLimitMb  Int?
  Points         Decimal? @db.Decimal(18, 2)
  SortOrder      Int?
  Exam           Exam?    @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model UserRole {
  UserId     String    @db.UniqueIdentifier
  RoleId     String    @db.UniqueIdentifier
  AssignedAt DateTime? @db.DateTime2
  Role       Role      @relation(fields: [RoleId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User       User      @relation(fields: [UserId], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@id([UserId, RoleId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ExamClass {
  ExamId     String    @db.UniqueIdentifier
  ClassId    String    @db.UniqueIdentifier
  AssignedAt DateTime? @db.DateTime2
  DueDate    DateTime? @db.DateTime2
  Class      Class     @relation(fields: [ClassId], references: [Id], onDelete: Cascade, onUpdate: NoAction)
  Exam       Exam      @relation(fields: [ExamId], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@id([ExamId, ClassId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model Appeal {
  Id           String      @id @default(uuid()) @db.UniqueIdentifier
  SubmissionId String?     @db.UniqueIdentifier
  StudentId    String?     @db.UniqueIdentifier
  Reason       String?
  Status       String?
  CreatedAt    DateTime?   @db.DateTime2
  ResolvedAt   DateTime?   @db.DateTime2
  Resolution   String?
  Submission   Submission? @relation(fields: [SubmissionId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
  User         User?       @relation(fields: [StudentId], references: [Id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model PendingEnrollment {
  Id           String    @id @default(uuid()) @db.UniqueIdentifier
  UserId       String    @db.UniqueIdentifier
  SemesterCode String?
  Season       String?
  ClassCode    String?
  SubjectCode  String?
  Status       String?   @default("Pending")
  CreatedAt    DateTime? @default(now()) @db.DateTime2
  UpdatedAt    DateTime? @updatedAt @db.DateTime2
  User         User      @relation(fields: [UserId], references: [Id], onDelete: Cascade)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model PublishedAssignment {
  Id          String   @id @db.UniqueIdentifier
  Version     String?
  Title       String?
  Description String?  @db.NVarChar(Max)
  ProjectType String?
  BlueprintId String?
  Data        String?  @db.NVarChar(Max)
  CreatedAt   DateTime  @default(now()) @db.DateTime2
  UpdatedAt   DateTime  @updatedAt @db.DateTime2
  IsDeleted   Boolean?  @default(false)
  DeletedAt   DateTime? @db.DateTime2
}

model SubmissionOverride {
  Id                 String    @id @default(uuid()) @db.UniqueIdentifier
  ExamId             String?   @db.UniqueIdentifier
  StudentId          String?   @db.UniqueIdentifier
  ClassId            String?   @db.UniqueIdentifier
  ExtendedDueDate    DateTime? @db.DateTime2
  PenaltyMode        String?   @default("SYSTEM_DEFAULT")
  CustomPenaltyRate  Decimal?  @db.Decimal(18, 2)
  FlatPenaltyAmount  Decimal?  @db.Decimal(18, 2)
  ScoreCap           Decimal?  @db.Decimal(18, 2)
  Reason             String?
  CreatedBy          String?   @db.UniqueIdentifier
  CreatedAt          DateTime? @default(now()) @db.DateTime2

  Exam               Exam?     @relation(fields: [ExamId], references: [Id], onDelete: Cascade, onUpdate: NoAction)
  Student            User?     @relation("SubmissionOverride_StudentIdToUser", fields: [StudentId], references: [Id], onDelete: Cascade, onUpdate: NoAction)
  Creator            User?     @relation("SubmissionOverride_CreatedByToUser", fields: [CreatedBy], references: [Id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([ExamId, StudentId], map: "SubmissionOverride_ExamId_StudentId_idx")
}

